---
"@app/web": patch
---

Refine backend data caching to correctly utilize credentials during cache population, resolve TypeScript type compilation errors on the user profile page, and redirect OAuth callback errors to the home page with graceful client-side alert handling.
